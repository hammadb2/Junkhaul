# Implementation Verification Matrix

Scope: clean worktree at `origin/main` commit `210afe0a3523da26d688831013e145c0e4ad7655`, plus this verification pass. The repository does not contain `JUNKHAUL_REHAUL_MASTER_BACKLOG.md`, so the matrix below maps the 25 launch-critical task areas from the request. No item is marked complete unless file and test evidence exists.

| Task | Requirement | Status | Implementation | Test | Manual QA | Problem | Resolution |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1. Master backlog source | Original 25-task backlog and acceptance criteria available | Blocked | Missing `JUNKHAUL_REHAUL_MASTER_BACKLOG.md` | None | Confirm source of truth | Cannot compare against absent file | Restore backlog file and rerun matrix line-by-line |
| 2. Release gate | Node 22/npm 10 clean install/build/test | Blocked | `.nvmrc`, `package.json#engines` | Local `npm ci` failed on Node `v21.6.0` | CI run on final commit | Local runtime not Node 22; dependencies absent | Run with Node `22.13.0`, npm `10.x` |
| 3. Mandatory integration CI | Release cannot pass by skipping integration | Partial | `.github/workflows/release-gate.yml` | Static diff only | GitHub Actions final run | Previously allowed skip | Patched release gate to require configured integration env |
| 4. Production Supabase evidence | Applied migration checksums/timestamps/RLS/indexes/backup | Blocked | `supabase/migrations/MANIFEST.json` | `npm run migrations:check` passed | Production SQL verification | No production credentials/project ref supplied | Run `docs/PRODUCTION_MIGRATION_EVIDENCE.md` queries on production |
| 5. Rehaul cart UX | Product add/remove/cart/reservation states | Missing | `app/rehaul/shop/[slug]/page.js`, `app/api/rehaul/cart/route.js` | None | 390/768/1440 screenshots and keyboard QA | Product form is unwired; no remove/cart UI | Build customer cart components and E2E tests |
| 6. Atomic reservation | One active reservation per item, race safe | Partial | `supabase/migrations/20260816000001_rehaul_commerce_hardening.sql#rehaul_reserve_listing_in_cart`, `lib/rehaulOrders.js#addToCart` | Not run; no DB integration test yet | Two-browser staging race | New RPC/index added, not verified against DB | Add and run concurrent reservation integration test |
| 7. Atomic order creation | Order, lines, cart, reservations transition atomically | Partial | `rehaul_create_order_from_cart`, `lib/rehaulOrders.js#createOrder` | Not run | Simulated DB failure/retry | RPC added; payment integration still absent | Add atomic failure tests and idempotency key |
| 8. Delivery pricing failure | Never convert pricing failure to free delivery | Partial | `lib/rehaulOrders.js#calculateOrderTotals`, `createOrder`, `rehaul_delivery_exceptions` | Not run | Admin exception approval flow | Zero-fee fallback removed; approval UI/API missing | Add authorized approval endpoint and tests |
| 9. Tax treatment | Versioned approved tax config and order-line evidence | Partial | `rehaul_tax_config_versions`, order/order item tax columns | Not run | Owner/accountant written approval | Runtime still uses unapproved zero placeholder | Load approved config and test rounding/totals |
| 10. Stripe authority | Server PaymentIntent, signed/idempotent webhook, reconcile | Partial | `app/api/stripe-webhook/route.js`, `lib/stripe.js`; no Rehaul-specific checkout PI found | Existing payment tests only generic | Stripe test mode transaction | Rehaul checkout creates order only, no PI/webhook reconciliation | Implement Rehaul PI + webhook state machine tests |
| 11. Premium storefront | Editorial home, browse/search, PDP gallery, responsive polish | Missing | `app/rehaul/page.js`, `app/rehaul/shop/page.js`, `app/rehaul/shop/[slug]/page.js` | None | Breakpoint screenshots | Inline starter UI, no real images/search/gallery | Build shared storefront design system and E2E visual QA |
| 12. Donation-to-inventory lifecycle | Full audited state machine | Partial | `lib/rehaulDonations.js`, `lib/rehaulInventory.js`, Rehaul migrations | Unit coverage superficial | Staging donation run | Transitions not proven server-authorized/idempotent | Add lifecycle integration tests and admin screens |
| 13. Clean-route enforcement | No landfill/dirty vehicle/unapproved items/capacity breach | Partial | `lib/dispatch.js#validateRouteFeasibility`, `lib/costLedger.js` | `tests/costLedger.test.js` partial | Physical clean-route manifest QA | Rehaul-specific manifest rules incomplete | Add clean-route policy engine and route E2E |
| 14. No-loss engine | Benchmark, all booking paths enforce authoritative quote | Partial | `lib/costLedger.js`, `tests/costLedger.test.js` | Benchmark unit passes only when deps available; migration tests ran | Mapbox staging quote/reconcile | Negative km test currently allows non-physical output; all paths not proven | Reject negative km; add path-level integration tests |
| 15. AI item evidence | Conservative evidence tiers, unsafe-underestimate reporting | Partial | `lib/itemEvidence.js`, `tests/itemEvidence.test.js`, item evidence migrations | Not run due missing deps | Human review and physical scale QA | No documented accepted threshold or full adversarial test set | Add benchmark suite and launch threshold |
| 16. Crew app reliability | Offline queue/device persistence/idempotent sync | Partial | `apps/crew_app/lib/src/data/offline/*`, unit tests | `dart format` passed; Flutter tests blocked by pub.dev DNS | Android + iOS airplane-mode journey | No real device evidence | Run hardware/simulator QA and attach logs |
| 17. Admin functionality | Complete responsive nav and all states | Partial | `app/admin/page.js`, many `app/api/admin/*` routes | `tests/phase5.test.js` superficial | Responsive admin QA | Single page/admin APIs do not prove full UI states | Build shared admin shell and state coverage |
| 18. Security matrix | Roles, RLS, IDOR, replay, private media | Partial | `docs/ADMIN_PERMISSION_MATRIX.md`, auth helpers, RLS migrations | Integration tests blocked | Role matrix staging run | Service-role-heavy APIs; full matrix not tested | Add permission integration suite |
| 19. Proper tests | Unit, DB/integration, E2E by domain | Partial | `tests/*.test.js`, `tests/integration/*` | `test:migrations` pass; others blocked | E2E staging | Rehaul commerce assertions insufficient | Add required DB/E2E tests |
| 20. Staging demo | Recorded end-to-end transaction and failure handling | Blocked | None | None | Required recording | No staging URL/credentials/test identities supplied | Run staging demonstration after gates pass |
| 21. Migration governance | Additive migrations, checksums, rollback/forward fix | Partial | `MANIFEST.json`, new forward migration | `migrations:check` passed | Supabase migration dry run | New SQL not applied to DB in this pass | Apply to staging first, then production with backup |
| 22. Auditability/idempotency | Financial/inventory/status changes audited and retry-safe | Partial | New RPC audit inserts; `lib/audit*.js` | Not run | Audit log review | Many transitions still not idempotent | Add idempotency keys and transition audit tests |
| 23. Media/secrets | No service-role or private media exposure | Partial | `scripts/secret-scan.js`, signed photo route | `secret-scan` passed | Private media URL QA | Build output not available to scan | Re-run after successful build |
| 24. CI/staging URLs/screenshots | Successful Actions link, deployment URL, screenshots | Blocked | `.github/workflows/release-gate.yml` | No final CI run available locally | Browser visual capture | No deployed final commit | Push branch and run CI/deployment |
| 25. Launch recommendation | Honest launch gate recommendation | Complete | `docs/KNOWN_LIMITATIONS.md` | Evidence in this matrix | Owner approval | Platform remains blocked | Recommendation: blocked for production, limited internal alpha only after Node/CI pass |

